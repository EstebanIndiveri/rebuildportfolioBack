<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h1 Buenos días Esteban este es un mensaje enviado desde tu página web!
        h2 Este mensaje es enviado por #{name}
        p Con el siguiente email: #{email}
        p #{message}